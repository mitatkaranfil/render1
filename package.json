{
  "name": "render1",
  "version": "1.0.0",
  "description": "Render deployment project",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm install && npm run build",
    "install": "npm install && cd frontend && npm install"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "path": "^0.12.7"
  }
}